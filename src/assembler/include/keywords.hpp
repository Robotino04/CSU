#pragma once

#include <vector>
#include <string>

const inline std::vector<std::string> keywords = {
    ".data",
    ".macro",
    ".address",
    ".org",
    ".asciiz",
};

const inline std::vector<std::string> instructions = {
    "subleq",
};